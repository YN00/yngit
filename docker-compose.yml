version: '3'

services:
#  nginx:
#    container_name: nginx
#    build:
#      context: Docker/nginx
#    restart: always
#    ports:
#      - 8888:8080
#    depends_on:
#      - vue
#
  vue:
    container_name: vue_app
    build:
      context: Docker/Vue
    restart: always
    ports:
      - 8081:8080
    volumes:
      - .:/app
    environment:
      TZ: Asia/Tokyo
    stdin_open: true
    tty: true

#  spring:
#    container_name: spring_app
#    restart: always
#    build:
#      context: .
#    ports:
#      - 5000:8080